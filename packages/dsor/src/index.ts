// The DSoR reference implementation starts here, and nothing is implemented yet.
// docs/status.md is the only authority on what is built.
//
// The first thing to build is stage 1 of docs/learn/learning-path.md:
// one entity, one query, one command, permissions, and an audit trail.

/** The specification version this implementation targets. */
export const TARGET_SPEC_VERSION = "1.4.0";
